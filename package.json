{
  "name": "webgpt-bridge",
  "version": "0.3.4",
  "description": "A private desktop bridge from WebGPT to local MCP agents.",
  "private": false,
  "main": "src/main.cjs",
  "author": "WebGPT Bridge",
  "license": "MIT",
  "scripts": {
    "start": "electron .",
    "build:icon": "node scripts/build-icon.cjs",
    "prepare:tunnel-client:mac": "node scripts/prepare-tunnel-client.cjs darwin-arm64",
    "prepare:tunnel-client:win": "node scripts/prepare-tunnel-client.cjs windows-amd64",
    "prepare:agent": "npm --prefix agent-runtime ci && npm --prefix agent-runtime run build && npm --prefix agent-runtime run build:native",
    "verify:desktop": "node scripts/verify-desktop.cjs",
    "pack": "npm run build:icon && npm run prepare:tunnel-client:mac && npm run prepare:agent && electron-builder --dir",
    "dist:mac": "npm run build:icon && npm run prepare:tunnel-client:mac && npm run prepare:agent && npm run verify:desktop && npm --prefix agent-runtime run acceptance && electron-builder --mac dmg zip --arm64 --publish never",
    "dist:win": "npm run prepare:tunnel-client:win && npm run prepare:agent && npm run verify:desktop && npm --prefix agent-runtime run acceptance && electron-builder --win nsis zip --x64 --publish never",
    "dist:all": "npm run dist:mac && npm run dist:win"
  },
  "devDependencies": {
    "electron": "^40.0.0",
    "electron-builder": "^26.0.12"
  },
  "build": {
    "appId": "com.localagenthost.desktop",
    "productName": "WebGPT Bridge",
    "artifactName": "${productName}-${version}-${os}-${arch}.${ext}",
    "asar": true,
    "asarUnpack": [
      "agent-runtime/**/*"
    ],
    "npmRebuild": false,
    "directories": {
      "buildResources": "build",
      "output": "release"
    },
    "files": [
      "src/**/*",
      "agent-runtime/**/*",
      "!agent-runtime/.webgpt-bridge{,/**}",
      "!agent-runtime/.npm{,/**}",
      "package.json",
      "README.md"
    ],
    "extraResources": [
      {
        "from": "build/tunnel-client",
        "to": "tunnel-client"
      }
    ],
    "mac": {
      "icon": "build/icon.icns",
      "category": "public.app-category.developer-tools",
      "target": [
        "dmg",
        "zip"
      ],
      "hardenedRuntime": true,
      "gatekeeperAssess": false
    },
    "win": {
      "target": [
        "nsis",
        "zip"
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    }
  }
}
